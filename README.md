# Volumio-Smart-Playlists
A bash script for Volumio 3 that automatically builds native Volumio playlists from a plain text file of rules - artist lists with optional AND/OR filters on album, genre, year, title, track number, duration, and BPM, plus optional deduplication of repeated tracks.
